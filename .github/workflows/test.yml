name: Tests
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  unit:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        node: [20, 22, 24]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ matrix.node }}
          cache: npm
      - run: npm ci
      - name: Install archive tools on Windows
        if: runner.os == 'Windows'
        run: choco install zip unzip --no-progress -y
      - run: npm test

  browser-and-package:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chrome
      - name: Install archive tools on Windows
        if: runner.os == 'Windows'
        run: choco install zip unzip --no-progress -y
      - name: Install window management for native background browser tests
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends openbox wmctrl python3-xlib
      - name: Select isolated test browser
        if: runner.os == 'Linux'
        run: node -e "require('node:fs').appendFileSync(process.env.GITHUB_ENV, 'GIVILOOP_CHROME_PATH=/opt/google/chrome/chrome\n')"
      - run: npm run build
      - name: Diagnose windowless input on Linux
        if: runner.os == 'Linux'
        run: xvfb-run -a node scripts/windowless-diagnostics.mjs
      - name: Diagnose windowless input on Windows
        if: runner.os == 'Windows'
        run: node scripts/windowless-diagnostics.mjs
      - name: Test the installed package in a managed virtual desktop
        if: runner.os == 'Linux'
        run: |
          xvfb-run -a bash -e <<'TEST_DESKTOP'
          openbox > /tmp/giviloop-window-manager.log 2>&1 &
          giviloop_wm_pid=$!
          trap 'kill "$giviloop_wm_pid" 2>/dev/null || true' EXIT
          giviloop_wm_ready=0
          for attempt in {1..50}; do
            if wmctrl -m >/dev/null 2>&1; then
              giviloop_wm_ready=1
              break
            fi
            sleep 0.1
          done
          if [ "$giviloop_wm_ready" != 1 ]; then
            cat /tmp/giviloop-window-manager.log
            exit 1
          fi
          npm run test:package -- --browser
          node scripts/windowless-desktop.mjs
          TEST_DESKTOP
      - name: Test the installed package on Windows
        if: runner.os == 'Windows'
        run: |
          npm run test:package -- --browser
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          node scripts/windowless-desktop.mjs
      - name: Keep the checked package and verification evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: giviloop-checked-package-${{ runner.os }}
          path: |
            .giviloop/releases/*.tgz
            .giviloop/releases/release-checks.json
            .giviloop/releases/package-unit-tests.tap
            .giviloop/releases/package-browser-tests.tap
            .giviloop/diagnostics/windowless-desktop.json
            .giviloop/diagnostics/desktop-*.log
          include-hidden-files: true
          if-no-files-found: warn
